{
  "name": "msf-cli",
  "version": "0.0.1",
  "description": "MySyncFolders - sync files between folders based on watchman",
  "main": "index.js",
  "bin": {
    "msf": "lib/cli.js"
  },
  "files": [
    "lib/"
  ],
  "scripts": {
    "clean": "rm -rf lib",
    "build": "npm run clean && npm run compile",
    "compile": "tsc -d",
    "start": "npm run compile -- --watch",
    "release": "npm test && npm run build && standard-version && git push --follow-tags origin master && npm publish",
    "test": "echo \"no test specified\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rocwind/msf-cli.git"
  },
  "keywords": [
    "watchman",
    "sync"
  ],
  "author": "Roc",
  "license": "MIT",
  "dependencies": {
    "fb-watchman": "^2.0.1",
    "fs-extra": "^9.1.0",
    "wait-ready": "^0.5.2",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "@types/fb-watchman": "2.0.0",
    "@types/fs-extra": "9.0.9",
    "@types/node": "14.14.37",
    "@types/yargs": "16.0.1",
    "typescript": "4.2.3"
  }
}
